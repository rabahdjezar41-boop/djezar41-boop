# djezar41-boop
README.md (oui/non) — par défaut : oui LICENCE (oui/non) — par défaut : oui (si vous avez choiune si (oui/non) — par défaut : non MODÈLE(S) DE SIGNALEMENT (oui/non) — ar défaut : non — si oui, pour quelle stack ? (Node, Python, Go, Rust, Docker, autre) .gitignore (oui/no) — par défaut : yes (selon le langage) Autres options (facultatif)
